# AIS Motion — Font & Visibility Fix

Fixed the invisible editorial subtitle lines caused by the original `em` stroke rule.
The site now uses Plus Jakarta Sans consistently across headings, navigation, body copy,
buttons and interface labels, with stronger anti-aliasing and readable heading weights.

The stylesheet order was also cleaned so the theme is the final visual layer.
